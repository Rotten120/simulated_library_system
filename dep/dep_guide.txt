DEPENDENCIES USED
- PYINSTALLER (to build)
- MYSQL.CONNECTOR (to communicate with MySQLdb)

TO INSTALL THESE DEPENDENCIES
RUN TOOLS/INSTALL_DEP.bat

OR 

EXECUTE THIS CMD
```
pip install -r dependencies.txt
```